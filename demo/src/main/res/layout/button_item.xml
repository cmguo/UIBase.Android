<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:xhb="http://schemas.android.com/apk/com.eazy.uibase"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="com.eazy.uibase.widget.ZButton.ButtonType" />

        <import type="com.eazy.uibase.demo.components.basic.ZButtonFragment.Style" />

        <import type="com.eazy.uibase.demo.components.basic.ZButtonFragment" />

        <variable
            name="data"
            type="ButtonType" />

        <variable
            name="style"
            type="Style" />

    </data>

    <androidx.appcompat.widget.LinearLayoutCompat
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/title"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:paddingStart="5dp"
            android:singleLine="true"
            android:text="@{data.toString()}"
            android:textFontWeight="3"
            android:textSize="18dp" />

        <com.eazy.uibase.widget.ZButton
            android:id="@+id/button"
            style="@style/ZButton"
            xhb:buttonType="@{data}"
            xhb:buttonSize="@{style.sizeMode}"
            app:loading="@{style.loading}"
            android:enabled="@{!style.disabled}"
            android:onClick="@{(view) -> style.testButtonClick(view)}"
            android:text="@{style.text}"/>

    </androidx.appcompat.widget.LinearLayoutCompat>

</layout>